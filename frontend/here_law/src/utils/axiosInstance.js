import axios from "axios";

// const API_LINK = import.meta.env.VITE_API_RUL;

// const axiosInstance = axios.create({
//   baseURL: API_LINK,
//   withCredentials: true,
// });

// 사용할 때는 이런식으로 한다.
// const fetchUserList = async () => {
//     try {
//       const response = await axiosInstance.get('/users'); // 다른 경로
//       // 처리 로직
//     } catch (err) {
//       // 오류 처리
//     }
//   };
