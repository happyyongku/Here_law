function MagazineMainCustomCard({ title, date, description }) {
  return (
    <div className="magazine-main-custom-card">
      <img src="" alt="" />
      <div>{title}</div>
      <div>{date}</div>
      <div>{description}</div>
    </div>
  );
}

export default MagazineMainCustomCard;
